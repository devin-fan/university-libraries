<h1>Upload Film</h1>

<div class="row">
    <div class="col-sm-12">
        <%= render 'form' %>
        <%= link_to(new_tag_path(film_id: @film.id), id: 'new_tag_link', class: 'button tiny radius', remote: true) do %> 
            Add New Tag 
        <% end %>
    </div>
</div>

<%= link_to 'Films', films_path %>
